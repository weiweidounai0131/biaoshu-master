interface:
  display_name: "Biaoshu Master"
  short_description: "跨行业标书与方案竞标的分阶段AI编写、审校与交付工作流"
  default_prompt: "Use $biaoshu-master to turn my tender materials into a traceable, reviewable, delivery-ready proposal for any industry; check the skill version first, keep background facts authoritative and references optional, follow the confirmed outline and Stage-4 writing rules, run AI recheck before human review, keep project data local, and wait for each required confirmation."
policy:
  allow_implicit_invocation: false
