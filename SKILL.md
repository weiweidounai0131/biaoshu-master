---
name: biaoshu-master
description: 面向政府部门、事业单位、国企、央企及其他政企客户的主标或陪标类标书分阶段协作工作流。适用于技术服务、咨询运营、信息化建设、系统集成、运维保障、培训实施、采购服务等项目；入口区分背景资料与参考资料并保持写作边界，支持需求与评分证据分析、目录和页数预算、分批Word生成、图片规划、逐图AI生图提示词、人工整合、逻辑一致性检查与终审定稿，并支持最终交付后的显式本机生图流程。仅当用户在当前消息明确引用本技能名称、`$biaoshu-master`或其路径时启用，不因对话仅涉及标书而自动调用。强制在每个大流程结束后停止并等待用户明确确认，不得列出阶段后在同一轮一口气全部执行。
---

# Biaoshu Master 标书协作工作流

## 显式调用前置条件（硬规则）

本技能只允许显式调用。只有用户在当前消息明确写出`$biaoshu-master`、`biaoshu-master`、本技能路径，或明确说“使用主标技能”，才允许加载和执行本技能。不得因为用户提到标书、招标、评分点、Word或类似项目，就根据对话内容自行调用本技能。已经由用户显式启动且仍在同一未结束工作流中的连续步骤，可按本技能的确认闸门继续；这不构成新的隐式调用。

## 显式调用后的 GitHub 更新检查

用户显式调用本技能后，第一步必须运行`python3 scripts/check_skill_update.py`检查版本。检查脚本以技能包内的`skill-version.json`为本地版本，以`skill-update.json`配置的公开HTTPS版本端点为远端版本，优先读取GitHub Contents API并以后备的`refs/heads`原始文件端点重试，不依赖本地`.git`，因此Git仓库安装和ZIP安装使用同一套逻辑。该检查只读取远端状态，不自动修改本地文件，也不会上传项目资料。若结果为`update_available`，先停止后续流程并询问用户：“GitHub上的biaoshu-master有新版本，是否先更新？回复‘是’更新，回复‘否’继续当前任务。”

- 用户明确回复“是”后，只有工作区干净时才执行脚本的`--update`；Git安装执行快进更新，ZIP安装从公开下载地址安全下载、校验并替换技能包。更新完成后提示用户重新显式调用`$biaoshu-master`并重新提交需求；更新前后的技能文件、项目资料和确认回执不得混用。
- 用户明确回复“否”后，记录本次选择并继续当前需求；不能再次强制询问同一版本。
- 用户没有回复、回复不是明确的“是/否”，或远端不可访问、当前技能包未配置公开端点时，不执行更新；前者保持等待，后两者说明检查结果后继续当前任务。
- 禁止把项目目录、背景资料、参考资料、确认回执、生成的Word/Excel或本机运行数据推送到GitHub。GitHub只用于发布技能源代码、说明文档和不含隐私的示例。

## 首次调用回复词

首次调用本 Skill 时，先向用户简要说明：

> 我会按确认台的分阶段流程协作完成标书：先确认项目资料与“主标/陪标”定位，再建立评分响应和页数预算，确认章节框架与图片规划，获得写作授权后分批生成 Word，并逐批进行审阅、页数复核和审校版迭代，最后完成全部交付确认。主要交付物为用户选择数量的 1 至 5 个 Word 标书文件，以及 1 个图片规划 Excel；图片规划表会包含每张图可直接交给本机生图模型的 AI 生图提示词。G0 至 G7 确认与正文交付阶段默认不生成、不插入图片，只有在最终交付汇总后用户明确回复“继续”，才进入示例图确认和 1 至 5 批的可选本机生图流程。本 Skill 经过多轮测试，在 Claude、GPT、GLM、MiniMax、DeepSeek 等模型上表现良好，建议尽量使用上述模型运行。

这段说明只在首次调用或用户明确询问使用方式时发送一次；之后按当前阶段闸门继续工作，不重复整段介绍。

## 核心定位

入口先由用户选择“标书定位”，并分别确认背景资料与参考资料。主标内容必须准确、扎实、可执行、前后一致，并对需求书和评分点形成充分但自然的点对点响应。陪标跳过AI页数预判并默认预填100页，用户可增减，整体保持合格但不追求主标精度，并选择2至3个不重要的评分点不写。两种定位都不得靠套话、重复改写、机械表格或为标题层级整齐而拆分内容。

将用户提供的会议纪要、内部方案和往期材料作为理解与写作依据，但投标正文只呈现合法、独立、对外的项目方案，不出现“根据会议”“需求书提到”“内部资料显示”“为满足评分”等制作痕迹。不得虚构人员、公司、证书、业绩、系统、数据或客户授权。

入口资料分为两类且全流程不得混用：背景资料是需求书、招标文件、评分表、澄清文件和客户正式资料，决定项目事实、需求、数字、时限、评分响应和承诺边界；参考资料是历史项目、成熟策略、公司经验和类似标书，只提供可选的方法与表达参考。参考资料不是项目事实，采用前必须核对与背景资料和用户确认口径一致；不得直接带入其中的客户、人员、证书、业绩、数字、日期或承诺。

## 前端确认优先规则

- 用户在确认台中修改并提交的字段就是后续任务的权威要求；即使与技能默认值不同，也必须直接按前端回执执行，不得反问用户是否确定，不得要求用户在聊天中重复确认。
- 只有内容违法、技术上不可能完成、回执损坏或不同已确认字段彼此矛盾到无法执行时，才报告具体阻塞；不得把“不同于默认设置”当作阻塞。
- 陪标页数的默认值为100，网页必须允许用户增减；用户确认后的目标页数是后续框架和生产阶段的权威值。
- “本次标书的额外注意事项”默认包含“评分表的商务部分不用写”。陪标另默认包含“内容不要精准，选择2~3个不重要的评分点不写”。这些是可编辑默认值；用户提交后的最终文本优先，后端不再追问。

## 强制确认闸门

### 不可违反的执行规则

- 一个用户回合默认只完成一个大流程。
- 每个大流程结束时，交付本阶段可核验结果、风险和待确认项，然后停止。
- 只有用户明确回复“确认”“继续”“没问题”或提出本阶段修改后，才进入下一大流程。
- 不得因为计划已经拆解，就在同一轮自动继续后续阶段。
- 不得把“正在等待确认”写在评论后继续调用后续阶段工具。
- 用户要求局部修改时，只修改当前阶段或指定文件，重新交付并再次等待确认。
- 用户说“继续”时，只推进到下一个已约定闸门；除非用户明确授权“连续做到第X阶段”“这几步不用确认”或“全部一口气完成”。
- 工具长时间运行时可以持续汇报进度，但进度汇报不等于闸门确认。

### 闸门状态

在工作底稿中维护当前状态：

`G0资料接收 → G1项目口径 → G2评分矩阵 → G3目录页数 → G4正文批次 → G5图片 → G6整合校正 → G7终审`

只允许从当前状态进入下一个状态。用户回退修改时回到相应闸门，不跳过重新确认。

## 本地确认台

调用技能后，先使用本地浏览器确认台弹出一次“项目背景与资料”确认窗，并在弹窗中单选主标或陪标，再依次完成项目口径、标书框架、图片规划和最终写作授权。该弹窗是正式流程前的阻断式入口，不是`00`、`G00`或新的可见步骤，也不出现在左侧阶段导航。读取[确认台协议](references/confirmation-ui.md)，严格按其中的数据文件、启动、等待、回执和回退规则执行。

### 浏览器优先与聊天回退硬规则

本地确认台是默认且必经的流程，适用于 Codex、WorkBuddy 及其他宿主。AI 不得因为“当前没有浏览器点击工具”“无法代替用户点击”或自行判断环境没有浏览器，就直接改用聊天确认。

每次首次进入流程时，必须在同一个前台命令中实际执行 `python3 scripts/bid_confirm_ui/server.py <project_dir> --daemon --wait --wait-stage intake --wait-timeout 0`。该命令会启动服务、尝试打开浏览器并直接阻塞等待入口回执；输出中的`browser_opened=true/false`必须记录并核对。不得传入`--no-browser`，不得拆成后台任务后再回复用户。自动打开失败时只能报告真实失败原因和本地地址，不得把“AI不能点击浏览器”当成失败理由，也不得擅自改成聊天确认。

**浏览器通知：** 确认台在用户首次点击入口“确认并开始分析”时，可请求 Chrome 的本地通知权限；用户允许后，页面在实际切换到下一阶段前发送一次通知。通知只适用于 Chrome 内核的 macOS 和 Windows，拒绝、无痕模式或系统通知关闭时必须静默降级，不能阻塞确认、等待或页面切换。

**阶段运行守卫与页面恢复：** 每次生成`stage1`至`stage4`推荐前，必须先运行`python3 scripts/stage_runtime_guard.py <project_dir> --stage <stage>`并遵守其输出；不得用先前回合记忆替代。每次`--wait`或`--wait-only`会再次执行服务端守卫并输出该等待阶段的硬规则。确认台页面通过本地心跳上报存活状态；进入阻塞等待前，如页面心跳已失联，服务端会尝试打开当前项目原确认台地址。心跳存在时绝不重复开页；失联后的补开设有三分钟冷却。该机制只能判断页面是否仍与本地服务通信，不能读取操作系统的浏览器窗口列表。

同一项目确认台已在运行时，只能附着到现有服务并复用其地址；AI不得再通过浏览器工具、`open`命令或重复`--daemon`手动打开该地址。确认台仅在首次成功启动时由独立的本地服务子进程自动打开一个页面；启动命令本身只负责健康检查，绝不重复打开页面；后续重试、恢复和阶段切换均在该页面内自动更新。

**stage1→stage2 前台硬门：** 这两个阶段最容易被宿主误转为后台。生成`stage1-recommendations.json`或`stage2-recommendations.json`并完成原子校验后，必须立刻执行对应的`--wait-only --wait-stage stage1|stage2 --wait-timeout 0`命令，并让该命令占用当前前台执行链。调用工具时不得填写或隐式启用`run_in_background`、`background`、异步、分离或“稍后轮询”选项；命令返回有效回执后才能生成下一阶段。若宿主报告命令已后台化，必须在同一轮立即重新以前台方式续等，不得把网页确认改成让用户回聊天发送继续。

仅以下两种情形可以改用聊天确认：用户明确要求“在聊天中确认”；或确认台启动失败，且重试后本地健康检查仍失败。后者必须在项目日志及 `<project_dir>/bid_confirm_ui/fallback-record.json` 中记录已执行命令、失败时间、错误信息和健康检查结果。聊天确认必须覆盖相同字段，不得减少确认内容。

### 同一对话连续运行硬规则

从入口确认台启动到第4阶段授权完成，必须视为**同一条持续执行链**，不是四次聊天任务。每一个`--wait`或`--wait-only`命令都必须以前台阻塞方式运行，禁止传入`run_in_background=true`、`background=true`、异步任务参数或创建独立后台任务，禁止轮询后立即结束，或仅发送“请在网页提交”“我在等你确认”后结束本轮回复。不得自行声称某宿主“回合制而不能等待”并把等待拆成“网页提交后回聊天”；入口必须使用`--daemon --wait`单命令启动并等待，后续才使用`--wait-only`续等。

等待命令以退出码 `0` 返回，或检测到对应有效回执后，表示“立即读取回执并继续生成下一阶段”，不表示任务完成。AI 必须在同一条对话、同一次持续运行中：读取已确认回执 → 写入下一阶段推荐 → 对既有确认台执行下一次 `--wait-only`。页面提交后用户不需要回到聊天发送“已提交”“继续”等消息。

若等待命令以退出码 `5` 返回，表示用户已重新开启更早阶段、旧等待已被确认台撤销；这不是超时或失败。AI 必须立即停止处理旧阶段，读取当前`workflow-state.json`，从该文件的`active_stage`恢复生成和等待，绝不能继续等待或要求旧阶段回执。

阶段2等待可能以退出码 `6` 返回，表示确认台写入了与当前阶段2推荐哈希绑定的 `stage2-rebalance-request.json`。这不是用户确认、不是失败，也不是回退。AI必须立即读取该请求，按其中的一级章节页数重新生成完整的 `stage2-recommendations.json`，保留当前阶段1回执哈希、写入相同的 `rebalance_request_id` 并完成结构校验，然后重新以前台方式执行 `--wait-only --wait-stage stage2 --wait-timeout 0`。不得用伪造 `stage2-confirmation.json` 唤醒等待。

阶段2或阶段3等待可能以退出码 `7` 返回，表示收到对应的`stage2-ai-adjust-request.json`或`stage3-ai-adjust-request.json`。这不是用户确认、不是失败，也不是回退。AI必须立即读取请求中的`instruction`和`current_data`，结合当前阶段的有效上游确认重新生成完整推荐，保留上游回执哈希并写入匹配的`ai_adjust_request_id`；生成完整、校验通过后再以`generation_status: complete`落盘，并重新以前台方式等待同一阶段。不得沿用旧推荐、直接修改确认回执或跳过用户再次确认。

仅在以下情况可以向用户发送一次简短进度消息：确认台首次可用时给出访问地址；真实启动/健康检查失败时报告失败；或第4阶段授权完成、生产与审校页已切换时报告状态。除此之外，不得把阶段性等待、阶段性确认或已生成推荐作为终止本轮执行的聊天答复。若宿主不能保持前台等待，必须使用该宿主的“等待/挂起/恢复当前任务”能力；没有实际验证该能力前，不得声称全流程网页回调已打通。

### 阶段交接自检与中断恢复

“准备读取”“接下来生成”“将继续处理”只是计划，不能写成已完成动作；只有本轮实际调用工具或脚本并取得结果后，才可声明已读取、已生成或已进入下一阶段。每次等待回执返回后，先检查对应下一阶段推荐文件确已写入并可被确认台读取，再继续下一次等待；在此之前不得结束当前对话。

每个`stage1`至`stage4`推荐文件必须使用原子写入，并在生成期间保持`generation_status: generating`；只有全部内容生成、结构校验和上游哈希绑定均完成后，最后一次原子写入才改为`generation_status: complete`。禁止先写空骨架或部分内容却标记`complete`。`--wait-only --wait-stage <stage>`会拒绝未完成推荐；前端只有在完整推荐哈希与该等待进程登记的哈希一致时才进入下一页。

技能重新被调用时，必须先读取`<project_dir>/bid_confirm_ui/workflow-state.json`、`callback-events.jsonl`和各阶段回执/推荐文件，判断是否存在“上一阶段已确认、下一阶段推荐尚未写入”的中断状态。发现该状态时，先恢复生成缺失的下一阶段推荐，再启动或附着确认台；随后明确告知用户：已从上次确认点恢复，请刷新仍打开的确认台页面。若页面仍保持打开，确认台会自动检测并跳转；若页面关闭或停在旧页，用户刷新一次即可，不需要重复确认前序阶段。

确认台与AI的职责边界：AI先从用户调用技能时的对话背景以及已提及、已上传文件中提取项目背景和本地资料路径，预填入口弹窗；网页只负责展示、补充、校验和保存一次性入口回执，不上传或复制文件，也不在浏览器中直接调用模型。若调用时没有足够背景或资料，用户直接在弹窗中填写背景并选择或粘贴本地路径。入口只提交一次；AI取得有效回执后读取资料、完成分析并生成阶段1推荐，阶段1页面随后自动出现，不再设置第二次资料确认或重分析流程。

本技能必须保持AI宿主中立，可由Codex、WorkBuddy或其他具备本地技能、文件读写和命令执行能力的AI调用。网页不得直接连接Codex任务、WorkBuddy会话、任何厂商模型接口或宿主专属插件。确认、写作、审校和回修一律通过项目目录中的版本化JSON、哈希绑定回执、等待命令和交付文件交换；当前调用本技能的AI就是本次生产执行者。详细遵循[生产与审校台协议](references/production-review.md)。

### 运行依赖（必须先完成）

安装或首次调用本技能后，当前AI必须先运行`python3 scripts/install_dependencies.py`，以清华PyPI镜像安装`python-docx`、`openpyxl`与`xlrd`；不得假定某个AI软件已内置这些库，也不得因缺库跳过Word、Excel或`.xls`评分表功能。完整说明见[INSTALL.md](INSTALL.md)。核心导出固定使用这套开源Python依赖；不得默认调用任何厂商内部npm包、MCP、插件或模型接口。

入口回执生成后不得在后续阶段修改背景、背景资料路径或参考资料路径；确需更换时，必须重新启动该项目流程。阶段1不提供资料选择、增删路径或再次分析入口。已完成步骤允许只读回看，不得因点击回看而自动回退。只有用户点击“修改本阶段”并再次确认影响范围后，才将该阶段及其下游数据按协议处理并回到编辑状态；仅回看不会使任何后续结果失效。

确认台四个页面必须以`/api/session`返回的统一工作流状态控制可用性，不得由各页根据局部接口自行推断；同时必须区分“后台流程当前进度”和“用户当前查看页”。红色高亮始终跟随当前查看页。已进入阶段4后回看阶段1时，阶段1红色高亮，阶段2至3保持绿色勾选与“已确认 · 只读回看”，阶段4显示为可点击返回的当前进度但不高亮；返回阶段4后阶段4再恢复红色高亮。回看不得改变后台流程状态。不得显示“进入”“可查看”等额外尾部状态。四页“修改本阶段”按钮使用统一组件样式和文案，只有二次确认修改时才改变工作流状态。

### 项目工作区管理

每次新建或恢复标书项目流程前，必须先运行`python3 scripts/project_workspace.py resolve`解析项目工作区，并读取返回的`project_dir`和`project_id`；后续`prepare_intake.py`、确认台、阶段推荐和交付台全部使用这两个值。未显式指定工作区时，默认根目录为`~/Documents/biaoshu-master-projects`，也可用`BIAOSHU_PROJECTS_ROOT`或`--root`更改。技能包、项目资料和交付物不放在同一目录。

工作区身份优先使用用户提供的`--project-key`，否则使用项目名称、招标人/客户和招标编号；若同时提供背景资料路径，还会使用其路径指纹辅助区分同名项目。不同项目创建不同的工作区目录；同一项目再次调用时返回原目录和原`project_id`。同名但资料指纹不同的项目不得静默复用；索引出现多个候选时必须要求明确的`--project-key`或`--project-dir`。如需接管旧版本已经存在的项目目录，使用`--project-dir`注册，不移动、不复制其中的资料。

“继续上次中断流程”和“重新开始一轮”必须严格区分：恢复时对同一工作区执行`prepare_intake.py <project_dir> --resume`，保留原入口和`run_id`；新一轮不传`--resume`，继续复用外层工作区，但将旧的确认状态归档到`bid_confirm_ui/history/`，将旧的`bid_delivery/`归档到`bid_delivery-history/`，再生成新的`run_id`。不得把同一个工作区同时当作两个不同项目使用。

### 专属生成规则制作模式

如果当前用户消息同时明确引用`biaoshu-master`技能，并包含“制作专属的标书生成规则”这一意图，必须切换到[专属规则制作模式](references/custom-rule-authoring.md)，不要进入G0至G7，也不要创建项目工作区或启动确认台。一次只引导用户完成领域、重点、硬约束、禁用内容和参考文件收集；先形成规则草案，得到用户明确确认后，才通过`scripts/rule_profiles.py register`写入`rules/custom/`并更新规则索引。默认规则`references/stage4-writing-rules.md`不可被覆盖。登记后按用户要求运行`set-default`，再回到普通对话等待下一次标书制作请求。

## 大流程

### G0：资料接收与读取计划确认

先完成对当前对话内容、已提及文件和已上传资料的提取，再运行`python3 scripts/project_workspace.py resolve --project-name "..." --client "..." [--tender-reference "..."] [--background-path <绝对路径>]`解析或创建项目工作区；读取返回的`project_dir`、`project_id`、`created`和`reused`后，再把项目背景、背景资料路径和参考资料路径写入`bid_confirm_ui/intake-recommendations.json`，最后启动确认台。入口由用户单选主标或陪标，选择结果写入`intake-confirmation.json`的`tender_position`并作为后续权威定位。使用返回的`project_id`运行`python3 scripts/prepare_intake.py <project_dir> --project-id <project_id> --background "..." --background-path <绝对路径> --reference-path <绝对路径>`生成入口数据；同类资料可重复使用对应参数，旧参数`--path`仅作为背景资料兼容别名。恢复中断轮次时改用同一`project_dir`执行`prepare_intake.py <project_dir> --resume`，不得同时提交新的背景或路径。入口弹窗默认展示这些内容；缺少时由用户在弹窗中一次补齐。文件选择只返回绝对路径，资料始终保留在本地，不发生上传或复制。不要根据文件名猜内容，也不要直接起草正文。

每次重新调用技能默认创建新的运行标识并把旧入口、工作流和阶段文件归档到`bid_confirm_ui/history/`；只有明确恢复同一轮中断流程时才给`prepare_intake.py`传入`--resume`。当前入口文件存在但回执哈希或运行标识失配时必须严格停在入口，不能使用旧阶段推荐或旧工作流状态。

**入口必须串行，禁止并行：** AI必须先完成背景、背景资料路径和参考资料路径提取，实际运行`prepare_intake.py`，读取生成的JSON并确认`prefill_ready: true`、两类路径已按对话内容分别填入且没有交叉重复；只有此校验通过后，才能首次运行`--daemon --wait --wait-stage intake`。不得一边思考/读取资料，一边启动确认台；不得先用空模板打开页面，再补写入口数据；不得因补写而重新打开或再次弹窗。确认台会拒绝没有`prefill_ready: true`的入口文件。

弹窗一次提交后生成`bid_confirm_ui/intake-confirmation.json`。AI读取该回执中的背景、`background_paths`和`reference_paths`，先以背景资料建立项目事实与评分底座，再将参考资料作为可选策略参考；不得把两类资料合并成无标签资料池。该入口不作为一个可见阶段，不增加`00`步骤；提交后只显示“AI正在分析”，阶段1推荐完成后自动进入项目口径页。

读取范围应覆盖：

- 资料清单和读取优先级；
- 本次标书范围：技术、服务、商务是否纳入；
- 背景资料与参考资料的清单、读取优先级及复用边界；
- 缺失文件或工具访问问题；
- 后续大流程和确认点。

入口提交是本阶段唯一一次资料确认。后续不再提供资料修改；确需新增、删除或更换资料时，重新启动项目流程。

### G1：项目口径与事实边界确认

读取正式需求书、招标文件、评分表、澄清文件和客户资料，建立项目卡。事实优先级：

1. 正式招标文件、需求书和正式澄清；
2. 用户明确确认的项目解释；
3. 最新评分表；
4. 对外正式方案与客户资料；
5. 会议纪要、内部资料和往期文件；
6. 保守通用判断。

其中，`background_paths`内的正式文件承担项目事实底座；`reference_paths`内的材料即使内容成熟，也只能提供候选策略、方法和表达，不能提升为项目事实或覆盖背景资料。

项目卡至少记录：项目名称、采购人、服务对象、服务期、业务目标、范围模块、数量与时限、人员要求、成果要求、版权、安全、承诺、最新版本口径、投标主体状态和不可写事项。

把事实标为：`确定`、`用户确认`、`通用方法`、`待确认`、`不可写`。当正式文件与会议早期口径冲突时，不自行选择；列出差异供用户确认。用户确认后的业务解释优先用于统一术语，例如地区层级术语的实际含义。

入口确认后，将项目卡写入项目目录的`bid_confirm_ui/stage1-recommendations.json`，并用`intake_confirmation_sha256`绑定当前入口回执。可先使用`python3 scripts/build_stage1_recommendations.py <project_dir>`生成具备默认排版和回执绑定的骨架，再由AI依据已读取资料完整填写项目、评分与事实边界字段；不得把未分析的空骨架当成最终推荐。确认台检测到有效推荐后自动显示阶段1：

```bash
# 启动页面后，当前AI必须立即在同一轮前台等待；两条命令之间不得回复聊天。
python3 scripts/bid_confirm_ui/server.py <project_dir> --daemon --wait --wait-stage intake --wait-timeout 0
# 入口确认返回后，读取资料、生成stage1-recommendations.json，再继续等待阶段1确认。
python3 scripts/bid_confirm_ui/server.py <project_dir> --wait-only --wait-stage stage1 --wait-timeout 0
```

网页默认填入AI提取的项目事实、标书概括、评分摘要和冲突项，并填入本技能的Word默认格式。用户可修改并补充注意事项。主标沿用技能原有目标页数建议；陪标跳过AI页数预判，目标页数默认预填100页但允许用户增减，确认值贯穿后续阶段。两种定位的额外注意事项默认加入“评分表的商务部分不用写”，陪标再加入“内容不要精准，选择2~3个不重要的评分点不写”。只有生成有效的`stage1-confirmation.json`后，才把其中内容作为后续权威项目口径并进入G2。**UI路径下，当前AI必须在同一条对话中阻塞等待这一回执；回执出现后立刻继续生成下一阶段，禁止要求用户额外发送“继续”。**

### G2：评分响应与材料矩阵确认

按评分表最新版本建立矩阵，而不是把评分表机械复制为正文。逐项列出：

- 评审因素、分值和完整得分条件；
- 正式需求对应条款；
- 正文应答章节及应答深度；
- 需要的表格、流程、承诺或交付物；
- 明确要求的证明材料；
- 已有、待补、无需提供和不可承诺状态；
- 可能冲突的人员、证书、时间和主体口径。

只有评分表或招标文件明确要求时才设置证明材料位。主标必须完整映射全部评分点；陪标应明确选择2至3个不重要评分点不写，其余评分点形成自然响应。人员证明材料集中在对应人员章节，不要在其他方案章节重复留空；其他章节涉及人员时只概括岗位机制。

未知投标主体统一写“我司”。具体人员、证书、社保和签章位置留空，并按用户要求用黄色底纹标注；不得编造。

交付矩阵，在此停止。

### G3：完整目录、颗粒度与页数预算确认

评分表C列等一级评分因素可以作为一级标题，但下级结构必须服从文章内容。先拟完整目录，再规划页数。

标题规则：

- 最多六级，只有内容确实需要时才下钻；
- 不强制每个二级标题都有三级标题；
- 一个标题下只有一段简短文字时，优先并入上级；
- 内容多、角色多、步骤多、异常多时可拆到四至六级；
- 同一层级的数量不追求整齐；
- 不把“本部分说明”“后续展开”“阶段衔接”等编辑提示作为标题；
- 文章逻辑优先于目录视觉对齐。

页数规划必须覆盖目标总页数并保留自然浮动。页数不足时增加真实颗粒度：场景、目标、角色、输入、步骤、工具、成果、边界、记录、异常、验证、接口和持续改进；不得用同义重复凑页。

同时规划内容形式：正文为主，列表、表格和图片用于辅助表达。默认每个一级章节的图片不超过3张，并在章首规划1张总览图；人员证明章节可不强行制作总览图，服从用户决定。

将完整一级至三级框架、各章页数预算、评分映射和深层细化授权写入`bid_confirm_ui/stage2-recommendations.json`。网页使用脑图式分栏展示，支持标题编辑、增删、同级排序、页数调整和节点展开折叠；页面另有“AI整体调整”输入，可直接要求AI重构当前整体目录。只有生成与当前G1回执绑定的有效`stage2-confirmation.json`后，才能进入图片规划。未提交时在此停止。

阶段2确认的一级至三级目录是正文生产的锁定骨架。阶段4不得自行新增、删除、改名或改编号；正文中新增的四级及以下标题必须挂在已确认的三级标题下，并且只能用于真实子主题展开。确需改变一至三级骨架时，必须回到阶段2重新调整并确认。

阶段2编辑页的“重新调整”用于只重分配一级章节页数。用户拖动章节边界后，确认台会写入`bid_confirm_ui/stage2-rebalance-request.json`，其中总页数必须保持不变、每章至少1页。当前AI必须在同一前台对话中读取该请求，基于项目资料和已确认口径重新分配对应章节下的二级、三级标题，并覆盖写入新的`stage2-recommendations.json`；新文件必须保留原`stage1_confirmation_sha256`、写入请求中的`rebalance_request_id`、更新`generation_status`为`complete`，且重新校验全部评分覆盖和章节页数。确认台检测到请求ID匹配的新推荐后自动回到阶段2编辑页；在此之前不得把旧框架当作完成，也不得跳到阶段3。

阶段2“AI整体调整”提交后写入`bid_confirm_ui/stage2-ai-adjust-request.json`，请求包含当前推荐哈希、用户的整体指令和页面当前编辑数据。当前AI收到阶段2等待命令退出码`7`后，必须读取该请求，结合项目底层资料和当前数据重生成完整阶段2推荐；不得把参考资料当作项目事实，不得只改标题而丢失评分映射、页数和需求边界。新推荐必须保留当前阶段1回执哈希、写入相同的`ai_adjust_request_id`并以`generation_status: complete`原子落盘，再重新以前台方式等待阶段2。用户随后确认的阶段2回执是目录、评分覆盖和篇幅预算的最新权威口径，覆盖阶段1生成的旧目录建议；它不修改阶段1项目事实回执。

框架确认后，AI依据已确认项目口径与框架生成`bid_confirm_ui/stage3-recommendations.json`。页面顶部“统一视觉方向”的配色、风格、背景、信息密度和避免风格必须由AI结合项目内容完整预填，作为用户可修改的图片风格建议，不得留空或写成待填写占位；`visual_direction`必须至少包含非空的`palette`、`style`、`background`、`density`和`avoid`字段，其中前四项为文字，`avoid`为至少一项的数组。新生成文件统一使用这些规范字段，不得只写`primary_colors`、`information_density`等旧别名；确认台仅为读取历史项目兼容别名，不会替代AI生成完整字段。主标的视觉方向须结合客户与项目背景，陪标须从随机视觉预设中选择并写入完整五项，不按客户品牌定向。AI在原子写入前必须逐项检查这五项，任何一项为空都不得标记`generation_status: complete`或进入等待。随后逐图规划图号、章节位置、图名、类型、用途或核心表达、核心节点、构图和方向，并为每张图生成`ai_prompt`：提示词只能依据已确认的图片字段和统一视觉方向，必须写清表达任务、核心节点、构图、画面方向与限制，不得引入未经确认的客户、人员、证书、业绩、数字、日期、Logo或承诺。浏览器允许用户增删和修改图片规划，但不调用模型或生图接口，不生成、不制作、不插入图片，也不记录成图路径；用户修改图片字段或统一视觉方向时，旧提示词必须失效并重建。页面另有“AI整体调整”输入，提交后由当前AI重新生成整个阶段3推荐，并再次完整生成视觉方向五项和逐图提示词。用户确认后生成有效`stage3-confirmation.json`，才可进入最终确认页。第3页未提交时必须停止，不得开始正文或图片制作。

**连续确认编排（UI路径强制）**：每个网页确认点均按“`--wait-only --wait-stage <当前阶段> --wait-timeout 0` → AI读取回执 → AI生成下一阶段推荐文件 → `--wait-only --wait-stage <下一阶段> --wait-timeout 0`”执行；等待命令返回不是任务结束信号。只有阶段4确认回执返回后，才进入正文生产与审校台。网页不调用模型；它只读取本地状态并在AI写入下一阶段文件后自动刷新。宿主若强制后台化命令，必须立即续等，不得结束对话。

最终确认页汇总项目口径、框架、页数、图片规划和风险边界，并让用户选择Word生成批次数。批次只能为1至5的整数，AI可按篇幅和一级章节给出默认推荐，但以用户最终选择为准；选择N次，最终必须交付恰好N个Word文件。页面同时明确固定交付1个图片规划Excel，内含逐图AI生图提示词。G0至G7确认和交付阶段仍禁止调用生图、交付图片文件或插入图片；只有最终交付汇总之后，才按“最终交付后的可选本机生图”规则处理显式用户授权。只有最终授权回执有效后才能开始G4；用户返回某一阶段点击“修改本阶段”时，该阶段及其下游推荐、回执必须归档失效，单纯只读回看不得改变状态。

### G4：正文Word分批生成与逐批确认

阶段3视觉预填规则：主标根据客户与项目背景生成视觉方向；陪标从随机视觉预设中生成视觉方向，不按客户品牌定向。两种模式都必须完整写入`palette`、`style`、`background`、`density`和`avoid`五项，且允许用户在阶段3页面修改；任何一项为空均不得进入等待或最终确认。

根据最终授权回执中的批次数生成Word。批次数只能是1至5；用户选择N次，必须按已确认的章节分组交付恰好N个Word，不得擅自增减。每批结束后必须先完成AI复校，再交付Word并等待人工确认。

阶段4授权后，**不得关闭前置确认台或要求用户打开新的地址**。先执行`python3 scripts/bid_delivery_ui/server.py <project_dir> --init`初始化并校验交付清单；Stage4页面的“生成规则”默认使用当前 Skill 的通用规则，也可以选择规则索引中的领域预设或用户自定义规则。用户选中的规则会随最终授权回执绑定，`--init`会把默认规则与选中的领域覆盖层固化为项目内的`bid_delivery/stage4-writing-rules.md`，并在`manifest.json`中登记规则摘要和规则ID。当前AI必须读取该项目快照后才能生成正文；每个`source/batch-NN.json`必须写入同一`writing_rules_sha256`，规则文件缺失、被替换或摘要不一致时不得继续生产。随后再以`--daemon --no-browser`启动生产与审校服务。该服务可以使用新的本地端口，但只作为后台承载；原确认页检测到服务就绪后必须用`location.replace()`在同一浏览器标签内切换到生产与审校页。只有用户在前序阶段实际点击“修改本阶段”并确认回退时，才归档生产台并将第4页恢复为最终确认页；单纯回看前序步骤不得改变第4页。AI不得只在聊天中生成Word后结束：每批正文必须先写入项目内的结构化权威稿，审校台从该权威稿展示正文、标题、表格、列表、图片占位和批次状态；Word是由权威稿导出的交付文件，不是浏览器编辑的唯一数据源。正文生产必须分别读取阶段1/入口回执中的`background_paths`与`reference_paths`：前者约束项目事实、评分和承诺，后者只提供经核对后可采用的策略与表达，不得直接混入客户、人员、业绩、数字或承诺。

Stage4写作不是“拿到资料后自由发挥”。正式生成任何正文前，必须完整读取[Stage4标书生成规则](references/stage4-writing-rules.md)，按其中的事实口径、评分实质覆盖、章节承接、纵向加厚、重复控制、Word格式、实际页数和渲染门禁执行。该规则统一的是论证逻辑，不是段落模板；用户当前明确指令和阶段确认结果优先，但不得用较低优先级参考资料突破背景事实和规则门禁。

当前生产与审校台已支持交付总览、批次导航、按内容块懒加载阅读、受控回修、Word导出和图片规划Excel导出。每个Word批次必须先写入符合协议的`source/batch-NN.json`，再由`scripts/bid_delivery_ui/export_word.py`生成Word、执行本地结构校验并登记哈希；登记后批次必然进入`ai_rechecking`，不得把聊天文本或旧Word直接当成权威稿。

**AI复校是人工审阅前的硬闸门：** 当前AI必须在每次Word导出登记后完整读取项目内`bid_delivery/stage4-writing-rules.md`、本批结构化源稿、Word和`results/word-batch-NN-validation.json`，重新核对规则硬约束、项目事实、阶段2一至三级骨架、评分覆盖、完全或近似重复段落、跨批术语与成果衔接、Word格式和页数预算。报告的`scope`必须包含`writing_rules`、`project_facts`、`outline_scoring`、`duplicate_control`、`cross_batch_consistency`、`word_export`和`page_budget`；每个问题都要写明位置、证据和处理建议，不能只写“已检查”。将完整报告写入临时JSON后，执行`python3 scripts/bid_delivery_ui/ai_recheck.py <project_dir> --batch <batch_id> --report <report.json>`。只有`status: passed`且没有`blocking`问题才会开放网页正文和确认按钮；`failed`会退回`regenerating`，当前AI修复并重新导出、复校。任何人工操作和最终确认都不能绕过这个闸门。

正文生成采用“目录先行、深度优先”的顺序：先按已确认的一至三级骨架建立本批标题树，再填充正文。页数不足时，严格按“扩展已有段落（讲透方法、步骤、角色、边界、验证和成果）→在已有三级标题下挂靠四级标题→必要时挂靠更深层标题→补充适合的表格或列表”执行。禁止追加无父级的独立模块、用空泛段落或靠连续增加标题凑页。每个新增标题都要能回答：它是否是真实子主题、是否能写出至少两块有逻辑的支撑内容、删除后原章节是否不完整。

生产校验会拒绝：三级标题未挂靠父级、四级及以下标题没有足够正文支撑、同一二级标题下超过7个三级标题、同一三级标题下超过7个四级标题，以及阶段2确认骨架被正文静默改写。低密度提示只用于提醒AI优先扩展已有内容，不自动要求把每个标题拆成相同数量的下级标题。

**页数是G4硬约束：** 以阶段2确认的各章页数预算和阶段4批次计划页数组织正文，不能只把计划页数作为元数据。每章应以评分权重、场景、流程、责任、SLA、风险、验收、表格和图片占位形成实质内容；不得用重复句、空泛承诺或无关段落凑页。导出前必须读取本地校验中的预计页数；若低于该批计划页数的90%，只扩充内容不足的章节后重新导出，不得登记为待审校。预计页数仅用于发现源稿偏薄，最终页数以WPS打开Word后的实际结果为准。

**页数审校迭代：** 每批确认交付后，审校台会显示页数校验区。导出器已经给出AI预计页数且预计值在计划页数`±10%`内时，用户查看Word后无需填写实际页数即可继续交付；只有用户发现WPS实际页数不足或超出时，才填写实际页数。服务会将WPS实际页数与本批原始预计页数写入项目级校准记录，后续批次自动使用该项目的实测比例修正预计页数；校准只改善预判，不替代WPS最终事实。服务会自动撤销该批“已交付”状态，生成`revision`事件并明确要求“扩充”或“压缩”，当前AI必须读取事件，执行`python3 scripts/bid_delivery_ui/begin_revision.py <project_dir> --batch <batch_id>`，针对章节实质性修改、重新导出并再次登记。若事件标记`replan_required: true`，必须先复核本批目录和章节粒度，禁止直接连续追加独立模块；需要改变一至三级骨架时回到阶段2确认。重新交付的文件名依次增加`—审校版1`、`—审校版2`等后缀，用户可重复审阅，直到确认最终版本。

**逐批阻塞是强制链路，不是聊天提醒：** 每批登记为`ai_rechecking`后，当前AI必须立即完成复校并登记`passed`回执；批次进入`ready_for_review`后，再以前台方式运行`python3 scripts/bid_delivery_ui/server.py <project_dir> --wait-only --wait-event user-action --wait-timeout 0`，并保持该命令运行。用户点击“确认并交付本批Word”后，该命令返回，AI须在同一持续对话中读取事件、生成下一批或应用AI回修并再次导出、复校、进入同一等待命令。`batch-confirmed`、`image-plan-confirmed`、`revision`和`final-confirmed`都是继续当前任务的事件，绝不是结束任务的信号。严禁在首批生成后以“请确认后再告诉我”“我将继续下一批”结束对话，也严禁把等待改成后台任务。

**宿主超时适配：** 某些AI宿主会在约两分钟后强制将前台命令转入后台。遇到这种转移时，当前AI必须立刻继续轮询或重新运行上述无限等待命令，且不发送收尾总结、不标记任务完成、不要求用户重新唤醒对话。只有读取到有效`final-confirmed`回执并完成交付汇总后，才允许结束本次对话。

当前生产与审校台已进一步支持两类受控审校：用户可对正文、列表、表格及图片规划记录做确定性直接修改，或提交需当前AI判断的改写请求；页数超差也沿用同一前台事件链。浏览器只将AI请求保存为项目目录下的请求和结果，不调用任何模型；人工直接修改由本地服务同步保存并确定性重导出，但导出后批次仍会重新进入AI复校，未通过前不得继续人工审阅或确认。AI收到改写请求或页数超差事件后必须核对当前源稿哈希，再写入结果、更新源稿、重新导出并复校，严禁用旧请求覆盖新版本。

图片规划Excel固定只输出1份、1个工作表，使用阶段3已确认的“图片名称、位置、核心表达、核心节点、构图建议和视觉要求”，并增加每张图的`AI生图提示词`。Excel是交给其他AI或最终交付后本机生图流程的输入，不得包含成图路径、成图状态或图片二进制；G0至G7不调用生图接口，也不插图。最终交付后的可选本机生图必须遵循[最终交付后生图协议](references/post-delivery-image-generation.md)。`scripts/bid_delivery_ui/export_image_plan.py`固定使用`openpyxl`导出，确保不同AI宿主获得相同的Excel；`@oai/artifact-tool`仅可在显式设置`BIAOSHU_USE_ARTIFACT_TOOL=1`时作为增强器使用，绝不构成依赖。

当前AI通过等待命令或在用户回到聊天说“应用网页修改”后读取本地修改回执，更新权威稿并重新导出Word。用户在网页确认一批后，系统将该批源稿和Word的摘要写入`confirmations/batch-NN-confirmation.json`，再解锁下一批；全部批次确认和图片规划Excel校验完成后，网页才开放最终确认。最终确认将恰好N份Word和1份Excel的文件摘要写入`confirmations/final-confirmation.json`并锁定当前版本，之后不得继续修改。

最终确认按钮固定放在审校台左侧图片规划 Excel 下方的独立操作区：未完成全部导出、批次确认和Excel校验时显示为灰色禁用；条件齐备后才可点击。点击后必须二次提示“将锁定交付物、不能返回网页修改、审校页面会自动关闭并停止本地服务”。用户确认后，网页提交`final-confirmed`回执，自动关闭或置空当前标签页并结束本地审校服务；当前AI必须保持等待该`final-confirmed`事件，读取最终回执及交付路径，向用户汇总N份Word和1份Excel后正式结束本次任务。

### 最终交付后的可选本机生图（不属于G0至G7）

完成最终交付汇总后，当前AI必须在回复末尾追加一句：“是否调用本机生图模型生成图片？回复‘继续’开始；回复‘否’或不回复则不生成图片。”这句话只提供一次选择，不得把用户沉默视为授权；只有用户明确回复“继续”才进入下面流程，任何其他回复均不启动生图。

1. 用户回复“继续”后，先校验`final-confirmation.json`和已确认的图片规划Excel，再运行`python3 scripts/bid_delivery_ui/image_generation.py <project_dir> start-example`。该命令生成首张示例图请求；当前AI只把已确认图片规划Excel和请求中的图片行、`ai_prompt`及统一视觉方向交给本机生图模型，不得把背景资料、参考资料、Word正文或未确认的项目事实一并推送。随后立即生成第一张图，作为视觉风格示例，不生成其他图片、不插入Word。
2. 将示例图展示给用户。用户明确提出“修改”或给出修改要求时，运行`revise-example`写入新的示例图请求，按照用户要求调整该图的视觉方向或`ai_prompt`后重新生成示例图；未收到用户确认前，不得进入批量生图。
3. 用户明确回复“确认”后，运行`confirm-example`，并询问：“接下来将剩余图片分几次生成？可回复1、2、3、4或5。”只有用户选择1至5中的一个整数才运行`set-batch-count`并开始批量生成。若剩余图片少于所选次数，按非空批次执行并保留实际批次数；示例图已计入图片清单，不重复生成。
4. 批量生成按请求文件逐批执行；每批只能生成该批列出的图片，并在生成后用`record-batch`记录本机绝对路径和文件摘要。生图结果只作为独立图片文件留存，不自动插入Word、不改变已确认的标书正文或Excel；本机模型不可用时不得自动改用外部服务，只保留Excel和可恢复的请求状态并向用户说明。

完整状态、请求文件和结果登记见[最终交付后生图协议](references/post-delivery-image-generation.md)。该流程必须在最终交付回执之后启动；若用户回复“否”、没有回复或没有明确回复“继续”，均保持最终交付状态不变。

写作规则：

- 正式投标口径，以“我司”陈述；
- 点对点覆盖评分项，但融入自然业务逻辑，不写成硬贴评分点；
- 前后复用同一项目名称、角色、对象、模块、时限、数字、术语和成果链；
- 不提内部会议、内部资料、撰写要求、页数规划和阶段制作过程；
- 会议形成的有效项目口径可直接写成项目方案，不披露来源；
- 背景资料是项目事实底座；参考资料仅在核对适用性后择优采用，不能直接作为项目事实；
- 不擅自把运营方法指标升级为合同承诺；
- 文字、列表、表格适量混用，避免每段“文字+表格”的机械模板；
- 图片先用独立红色占位提示，写清图名和逻辑；最终替图后删除占位提示；
- 分批文件不得保留“本章（续）”“本部分承接前文”“后续将在另一批展开”等合并过渡段。

用户未另行规定时，Word格式采用：

- 仿宋_GB2312，全部四号；
- 全部标题、正文、列表及表格文字颜色为黑色；
- 标题加粗、无首行缩进、段前10磅；
- 正文首行缩进2字符、1.25倍行距；
- 列表统一使用`·`；
- 表格无背景色、黑色单边框、表内文字居中、无首行缩进；
- 标题最多六级。

每批校验：

- Word可重新打开；
- 标题编号、层级和颗粒度自然；
- 阶段2确认的一至三级骨架完整且顺序一致；新增深层标题均有明确父级，三级和四级密度不超过7个；
- 已读取项目内Stage4标书生成规则，源稿写入的`writing_rules_sha256`与`manifest.json`一致；
- AI复校报告完整绑定当前Stage4授权、规则快照、源稿和Word摘要，`status`为`passed`且无`blocking`问题；
- 本地预计页数不低于计划页数的90%，实际WPS页数在计划页数±10%内；
- 字体、字号、缩进、行距、表格；
- 评分覆盖；
- 内部口径、过渡段、重复段和错词；
- 材料位仅存在于允许章节。

交付本批Word、实际页数、评分覆盖和差异，在此停止。用户确认后再生成下一批。

### G5：图片规划Excel交付

以阶段3图片规划回执和最终授权回执为权威数据，固定输出1个`.xlsx`图片规划表。该Excel至少包含：图号、一级章节、精确放置标题、具体放置说明、图片名称、图片类型、用途或核心表达、核心节点、构图建议、画面方向、是否章首总览图、统一视觉要求、应避免的风格、生图补充说明和`AI生图提示词`。每行提示词必须与该行图片规划一致，且只能依据已经确认的内容。

Excel用于用户交给其他AI或设计工具逐张生图。G5本身不得调用任何生图接口，不得生成或交付图片文件，不得记录成图路径，也不得把图片插入Word。最终交付汇总后的可选生图不属于G5，必须遵循下面的显式授权流程。若正文写作后新增、删除或移动图片位置，先更新图片规划清单并重新确认，再重新输出Excel；不得静默偏离已确认清单。

创建Excel时同时遵循表格技能，进行内容检查和视觉渲染校验，确保长文本换行、列宽、冻结标题、筛选和工作表结构可直接使用。

Excel也必须登记到生产与审校台的交付清单中，但阶段3已确认的图片规划是其内容权威来源。审校台只展示规划记录与文件状态，不调用生图、不展示伪造的成图结果。最终交付确认必须验证恰好存在用户选择数量的Word和1个图片规划Excel。

### G6：整合、替图与正式文件校正

用户手工合并或要求整合时，先复制原文件，输出非覆盖版本。执行：

- 恢复合并造成的标题样式丢失；
- 删除“续”、批次说明、章节过渡总结和制作要求；
- 检查章节完整、编号连续、缺节、多节和重复段落；
- 将图片插入正确段落，删除占位提示；
- 章首总览图移动到一级标题导语之后、首个二级标题之前；
- 核对黄色材料位只在允许章节；
- 按正式文件修正旧稿中错误或过强口径；
- 保留用户已确认且不受影响的内容。

交付整合修订版和“新增、删除、改正、移动、待补”差异表，在此停止。

### G7：终审与定稿确认

完整执行[终审检查表](references/review-checklists.md)，至少检查：

- 需求书与评分点逐项实质覆盖；
- 评分表最新版和用户确认口径；
- 项目名称、采购人、服务期、模块、区域、人数、证书、日期、时限和目标数字；
- 前后名词、角色、流程、成果、边界和承诺一致；
- 无内部会议、制作说明、评分反推、过渡段和模板残留；
- 无虚构人员、证书、业绩、组织、系统和客户数据；
- 材料位、黄色底纹和签章位正确；
- 标题颗粒度自然，最多六级，编号连续；
- Word格式、表格、图片和实际WPS页数满足要求；
- 图片中文字、节点和正文一致；
- 无完全重复长段落和人工合并残留。

正式输出终审定稿、实际页数和问题处置清单。除非用户明确要求，最后再导出PDF；在正文和图片均确认前不导出PDF。

## 阶段交付模板

每个闸门的回复只包含：

1. 本阶段完成结果；
2. 关键风险和缺口；
3. 需要用户确认的具体事项；
4. 下一阶段名称；
5. 明确说明“收到确认后再继续”，并真正停止。

需要评分矩阵、页数表、图片清单和差异表时，读取[工作模板](references/workflow-templates.md)。

## 工具与文件原则

- DOCX工作必须同时遵循文档技能；图片规划Excel必须同时遵循表格技能。G0至G7不调用生图接口；最终交付后的可选本机生图只允许在用户明确回复“继续”后按[最终交付后生图协议](references/post-delivery-image-generation.md)执行。
- 核心工作流不得依赖Codex任务API、WorkBuddy专属接口或某一家模型服务；宿主差异只体现在当前AI使用什么能力生成内容，项目状态和网页交互必须使用相同的本地文件协议。
- 优先用结构化读取和WPS集成校验Word；用户要求不导出PDF时不得以PDF作为中间校验步骤。
- 输出文件放入用户指定目录；未指定时建立项目专用文件夹，避免桌面散落。
- 原稿默认保留；修订稿使用清晰版本名。
- 不把结构抽取检查说成视觉检查；WPS实际页数和打开验证要明确说明。
- 使用[Word终审脚本](scripts/audit_bid_docx.py)进行可重复的结构、格式和残留词检查；脚本结果不能替代人工语义校验。
